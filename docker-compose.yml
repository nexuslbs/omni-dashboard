services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "12346:3001"
    environment:
      PORT: "3001"
      PGPASSWORD: omniagent
    volumes:
      - /opt/workspace/omniagent/data/:/opt/data/:ro
      - /opt/workspace/omniagent/data/user/:/opt/data/user:rw
      - /opt/data/agent-interactions.db:/hermes-data/agent-interactions.db:ro
      - /opt/data/state.db:/hermes-data/state.db:ro
    networks:
      - omniagent_default

networks:
  omniagent_default:
    external: true
