import { createPluginPage } from "../lib/plugin-list";
export const renderProviders = createPluginPage({
  type: "provider",
  title: "Providers",
  subtitle: "AI model providers — built-in and plugin-based",
});
